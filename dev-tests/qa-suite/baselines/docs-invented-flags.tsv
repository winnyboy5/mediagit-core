metric	value
invented_flags	4
# 108 -> 4 over the 2026-08-25 docs truth-up.
# All 4 remaining are FALSE POSITIVES of a per-page lexical gate, verified
# individually against --help:
#   bisect/clone/fetch cite `mediagit log --oneline` - real flag on log
#   reflog cites `mediagit reset --hard`             - real flag on reset
# Each is an accurate cross-reference to ANOTHER command. Rewording correct
# documentation to satisfy a metric would make it worse, so they stay.
#
# The earlier value of 7 also covered auth's --name/--permissions/--role,
# which were never invented: the gate recursed only ONE level into
# subcommands and could not see `auth key create` / `auth admin create-user`.
# That blind spot is now fixed (two-level recursion) and those 3 are gone.
#
# If this number RISES, something real was fabricated - investigate, do not relock.
